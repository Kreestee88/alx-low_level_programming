#include <stdio.h>
/**
 * main - program that prints all single digit numbers of base 10 starting fro
 * Return: 0 (Success)
 */

int main(void)
{
	int g;

	g = 0;
	while
		(g < 10) {
			putchar(g + '0');
			g++;
		}
	putchar('\n');
	return (0);
}
