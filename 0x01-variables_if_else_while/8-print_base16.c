#include <stdio.h>
/**
 * main - Hexadecimal
 * Return: 0 (Success)
 */
int main(void)
{
	char f;

	int g;

	f = 'a';
	g = 0;
	while
		(g < 10) {
			putchar(g + '0');
					g++;
					}
					while
					(f <= 'h') {
					putchar(f);
					f++;
					}
					putchar('\n');
					return (0);
}
