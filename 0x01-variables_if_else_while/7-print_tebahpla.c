#include <stdio.h>
/**
 * main - Smile in the mirror
 * Return: 0 (Success)
 */
int main(void)
{
	char b;

	b = 'z';
	while
		(b >= 'a') {
			putchar(b);
			b--;
		}
			putchar('\n');
			return (0);
}
