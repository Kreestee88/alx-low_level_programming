#include <stdio.h>
/**
 * main - a program that prints the alphabet in lowercase, then in uppercase
 * Return: 0 (Success)
 */

int main(void)
{
	char b;

	char c;

	b = 'a';
	c = 'A';
	while
		(b <= 'z') {
			putchar(b);
			b++;
		}
	while
		(c <= 'z') {
			putchar (c);
			c++;
		}
			putchar('\n');
			return (0);
		}
